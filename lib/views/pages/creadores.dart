import 'package:flutter/material.dart';

class Creadores extends StatelessWidget {
  const Creadores({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Creadores'),
      ),
      body: const Center(
        child: Text('Creadores'),
      ),
    );
  }
}
